#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vi = vector<int>;

int main()
{
    ios::sync_with_stdio(0);cin.tie(0);
    int n, x;
    cin >> n >> x;

    vi a(n);
    for (int i=0; i<n; i++)
        cin >> a[i];

    cout << 0 << "\n";

    return 0;
}

